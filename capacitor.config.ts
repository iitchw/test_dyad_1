import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.cosmickiwiburst',
  appName: 'cosmic-kiwi-burst',
  webDir: 'dist'
};

export default config;
